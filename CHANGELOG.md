# 更新记录

本项目遵循语义化版本。早于 `1.0.0` 的版本属于开发预览，数据格式与协议可能不兼容。

## 0.1.0 - 2026-07-29

首次架构尖峰：

- 确定“知织 / ZhiWeave”项目名；
- 采用 `AGPL-3.0-or-later`；
- 建立 Tauri 2 + React + TypeScript 跨平台客户端；
- 接入 CodeMirror 6 Markdown 编辑体验；
- 建立 Rust 领域、应用、Markdown 导入与 `ZHIWEAVE/1` 协议模块；
- 建立 Axum 独立服务端健康检查；
- 提供 Windows MSI 与 NSIS 安装包构建；
- 加入前端、Rust、Clippy 与 GitHub Actions 检查。

这一版本用于验证跨平台技术路线，不包含可用于真实数据的加密存储和同步。
