# 更新记录

本项目遵循语义化版本。早于 `1.0.0` 的版本属于开发预览，数据格式与协议可能不兼容。

## 未发布

- 笔记列表、导航、Wiki、反向链接和图谱打开现在复用单个临时预览标签；双击、显式打开或编辑
  会固定，标签右键和命令面板按当前状态提供固定/转为临时预览；
- 修复 480 px 及以下窗口中检查器覆盖后残留正文窄条的问题，面板现在完整占据正文表面且不产生
  页面级横向溢出；
- 缺失 Wiki 目标现在先展示将创建的标题、路径与可选标题段，只有用户明确确认后才由 Rust
  重新解析并以非覆盖方式创建，歧义或状态变化都会中止；
- Markdown 图片与 Wiki 附件嵌入接入固定工作区内的 Rust 权威解析器，支持经过签名、扩展名、
  大小和像素限制复核的 PNG、JPEG 与静态 WebP；
- 远程地址、活动 `data:`/`file:`/`asset:` URL、隐藏元数据、符号链接、路径逃逸、SVG、GIF、
  动画 WebP 和超限资源保持阻止或安全占位；
- 编辑与阅读视图均提供惰性附件预览、明确失败状态和附件专属右键命令；
- 修复 Live Preview 插件装饰跨越换行导致的运行时白屏，并隐藏标签栏无意义的滚动条。

## 0.1.1 - 2026-07-29

修复首次可交互体验：

- 让全部主导航真正切换学习区域与内容；
- 新增可用的新建学习内容窗口和结构化 Markdown 模板；
- 新增搜索、阅读/编辑切换、复制网页 AI 提示词；
- 新增手动版本保存、历史查看和可恢复回滚；
- 恢复旧版本前自动备份当前内容；
- 将笔记、任务状态和版本历史持久化到本机应用存储；
- 新增带二次确认的“重置演示数据”；
- 补充操作结果提示、空状态、键盘关闭窗口和模型测试；
- 移除不必要的全量代码语言包。

## 0.1.0 - 2026-07-29

首次架构尖峰：

- 确定“知织 / ZhiWeave”项目名；
- 采用 `AGPL-3.0-or-later`；
- 建立 Tauri 2 + React + TypeScript 跨平台客户端；
- 接入 CodeMirror 6 Markdown 编辑体验；
- 建立 Rust 领域、应用、Markdown 导入与 `ZHIWEAVE/1` 协议模块；
- 建立 Axum 独立服务端健康检查；
- 提供 Windows MSI 与 NSIS 安装包构建；
- 加入前端、Rust、Clippy 与 GitHub Actions 检查。

这一版本用于验证跨平台技术路线，不包含可用于真实数据的加密存储和同步。
